{
  "name": "json-merge-interactive",
  "version": "0.1.0",
  "description": "An interactive json merge tool",
  "main": "dist/index.js",
  "scripts": {
    "dev": "babel --watch --presets=es2015 -d dist src",
    "build": "babel --presets=es2015 -d dist src",
    "prepublish": "npm run compile",
    "test": "mocha"
  },
  "keywords": [
    "json",
    "merge"
  ],
  "author": "Ryan Tablada",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.9.0",
    "chai": "^3.5.0",
    "mocha": "^2.5.3"
  }
}
